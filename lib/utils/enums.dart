enum CurrentStatus {
  initial,
  loading,
  error,
  other,
  success,
  uploading,
  complete
}

enum CurrentStep { one, two, three, four, five, six, seven, eight, nine, ten }

enum UrlType { email, phone, web, map }

enum MessageType { info, success, error }

enum DialogType { info, yesNo, select }
